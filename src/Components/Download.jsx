

export const Download=({handledownload})=>{
return(
    <div className="button-container">
    <button  onClick={handledownload}className="button">
            Download
          </button>
      </div>
)
}